/**
 * Corese Graph-backed storage implementation.
 *
 * <p>All classes here depend on the legacy Corese {@code Graph} type. That
 * dependency must not leak into the storage manager API or other impl packages.</p>
 */
package fr.inria.corese.core.next.storagemanager.impl.graph;
