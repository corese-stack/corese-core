/**
 * SPARQL-specific pipeline implementation: AST, bridge, execution, and serialization.
 *
 * <p>All types in this tree are internal details. Public query contracts
 * live in {@code next.query.api} and must not depend on this package.</p>
 */
package fr.inria.corese.core.next.query.impl.sparql;
